const config = {
    mode: "jit",
    purge: ["./src/**/*.{html,js,svelte,ts}"],

    theme: {
        extend: {},
    },

    plugins: [],
    darkMode: "media",
    separator: "_",
};

module.exports = config;
